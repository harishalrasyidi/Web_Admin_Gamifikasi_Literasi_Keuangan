[phases.setup]
nixPkgs = ["php", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = ["composer install --optimize-autoloader --no-dev"]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"